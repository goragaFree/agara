package dev.lucid.event.impl;

import dev.lucid.event.Event;

/**
 * Сервер прислал обновление времени.
 *
 * <p>Ванильный сервер шлёт такой пакет раз в 20 тиков, то есть раз в секунду при здоровом
 * сервере. Поэтому промежуток между двумя событиями — единственное, что клиенту вообще
 * доступно для оценки TPS: настоящий TPS сервер не сообщает никогда.</p>
 *
 * <p>Данных в событии нет специально. Значение имеет ровно момент прихода, а его подписчик
 * и так узнаёт по факту вызова.</p>
 */
public final class TimeSyncEvent extends Event {
}
